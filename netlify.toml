[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20.18.0"
  VITE_API_BASE = "https://your-backend-domain.com"  # غيّرها إلى رابط API الحقيقي إذا توفر